# DashboardLayout

import { Playground } from 'dokz'
import { Stack } from '@chakra-ui/react'
import {
    DashboardLayout,
    Block,
    Breadcrumbs,
    SmartBreadcrumbs,
} from 'dashboard-blocks/src'

<Playground iframe={false}>
    <DashboardLayout p='20px'>
        <Breadcrumbs items={['User', 'Morse', 'Services']} />
    </DashboardLayout>
</Playground>

The smart breadcrumbs infer the breadcrumbs from the current url, you can pass a `basePath` to remove paths that come before that

<Playground iframe={false}>
    <DashboardLayout p='20px'>
        <SmartBreadcrumbs basePath='/docs' />
    </DashboardLayout>
</Playground>
