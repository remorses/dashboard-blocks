---
name: Getting started
---

import { Playground } from 'dokz'
import { Stack, Box, Image } from '@chakra-ui/core'
import {
    DashboardLayout,
    Block,
    SideNavLink,
    TableBlock,
    BannerBlock,
    Breadcrumbs,
    SmartBreadcrumbs,
} from 'dashboard-blocks/src'
import { ResponsiveBar } from '@nivo/bar'
import { barData as data } from '../../support'

# Dashboard Blocks

Dashboard Blocks is a set of components built to create beautiful dashboards

These components are made with composability and ease of use in mind, you should be able to create a dashboard page in less than 10 minutes and with less than 200 lines of code

Do you want to see an example? here is how you build a landing page in 5 minutes

<Playground iframe>
    <DashboardLayout
        bg='gray.50'
        p='20px'
        pb='100px'
        sideNavItems={[
            <SideNavLink href='/docs/components/DashboardLayout'>
                User
            </SideNavLink>,
            <SideNavLink href=''>Projects</SideNavLink>,
            <SideNavLink href=''>Settings</SideNavLink>,
        ]}
    >
        <SmartBreadcrumbs />
        <BannerBlock
            heading='Setup Dashboard Blocks'
            image={<Image h='160px' src='/3946760.svg' />}
            subheading={
                <Box>
                    Let's create your first project<br></br> Follow me to learn more
                    <br></br>
                </Box>
            }
        />
        <Stack spacing='6' direction='row'>
            <Block w='100%' />
            <Block w='100%' />
        </Stack>
        <TableBlock
            fontSize='16px'
            head={['Name', 'Surname', 'Age']}
            rows={[
                ['Name', 'Surname', '23'],
                ['John', 'Surname', '21'],
                ['Jim', 'Surname', '23'],
            ]}
        />
        <Block height='300px' heading='Chart of something' w='100%'>
            <ResponsiveBar
                data={data}
                keys={[
                    'hot dog',
                    'burger',
                    'sandwich',
                    'kebab',
                    'fries',
                    'donut',
                ]}
                indexBy='country'
                padding={0.3}
                labelSkipWidth={12}
                labelSkipHeight={12}
                labelTextColor={{
                    from: 'color',
                    modifiers: [['darker', 1.6]],
                }}
            />
        </Block>
    </DashboardLayout>
</Playground>
